These scripts are preserved as historical search and dataset-generation
experiments.

They are intentionally not part of the STS2AI self-contained mainline:

- do not treat them as supported entrypoints
- do not reintroduce them into mainline smoke by default
- restore or port them deliberately only if a future workflow truly needs them
