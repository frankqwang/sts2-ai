# 2026-04-24 归档说明

本目录保存 bridge 统一重构前后的过时文档。它们仍有历史背景价值，但其中的默认入口、协议、状态构建路径和训练主线说明不再代表当前实现。

当前实现依据见：

- `../../design/game-bridge-current.md`

归档内容：

- `visible-spectate-guide.pre-bridge-rpc.md`：旧 visible 观战 HTTP v2 指引。
- `sim-spectate-unify-plan.pre-shared-builder.md`：共享 builder 落地前的方案分析。
- `llm-finetune-plan.pre-runtime-state.md`：LLM 路线早期计划。
- `llm-handover-2026-04-24.pre-runtime-state.md`：卡牌动态描述修复前后的中间交接。
- `zero-rl-mainline.legacy.md`：旧 zero 主线说明。
- `zero-single-case-overfit.legacy.md`：旧 zero 单 case 实验说明。

读取这些文档时不要照搬其中的旧 endpoint、旧 JSON DTO、旧 step API 或 Python 替换卡牌模板方案。
