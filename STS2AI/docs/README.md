# STS2AI Docs

当前默认上下文只保留正在维护的两条线：

- `STS2AI/bridge/game_bridge` 与 `STS2AI/ENV` 的 sim / spectator bridge。
- `STS2AI/data/skada` 的数据抓取与明细构建。

## 当前有效文档

- `design/game-bridge-current.md`：sim pipe protobuf 与 spectator HTTP protobuf JSON 的当前架构。

## 归档

旧训练主线、旧 HTTP v1/v2 观战入口、旧 JSON DTO 状态链路、LLM 中间交接与实验计划已经归档到：

- `archive/2026-04-24/`

归档文档只保留历史背景，不再作为实现依据。
