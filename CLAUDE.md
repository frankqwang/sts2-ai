# CLAUDE.md

本文件保留最小协作约束，避免继续把旧训练体系带回项目上下文。

## 当前项目定位

- 当前只默认关注两条线：
  - `STS2AI/Python/game_bridge`
  - `STS2AI/Python/data/skada`
- 其它历史训练路线、旧架构分层、旧实验状态说明均视为过期信息。

## 工作原则

- 使用中文沟通与写文档。
- 优先修改 `STS2AI` 下的可维护代码，不主动改 `src`。
- 新文档放 `STS2AI/Docs`，不要在仓库根目录新增杂项文档。
- 运行产物、日志、临时输出放 `STS2AI/Artifacts`。
- 文本读写尽量统一为 UTF-8。

## 回答与引用

- 引用本地文件时使用带前导 `/` 的绝对 Windows 路径。
- 引用代码时尽量带行号，方便定位。

## 执行习惯

- 对耗时命令，默认后台执行。
- 回复中应明确：
  - PID
  - 日志文件
  - 输出目录
